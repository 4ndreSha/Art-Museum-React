import React from "react";
import { NavLink } from "react-router-dom";

import "@components/BookmarkButton/styles.scss";

const BookmarkButton = () => {
  return <button className="bookmarkButton" />;
};

export default BookmarkButton;
